server:
  port: 8080 #项目启动访问端口
spring:
  application:
    name: root@localhost
  datasource: #连接数据库配置
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/djq-estate?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
username: root
password: root

servlet:
  multipart: #文件上传配置
    max-file-size: 10MB
    max-request-size: 50MB